<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- 被选中时的颜色 -->
    <item android:drawable="@drawable/shape_img_share_indicator_bg_selected" android:state_selected="true"/>
    <!-- 获得焦点时的颜色 -->
    <item android:drawable="@drawable/shape_img_share_indicator_bg_selected" android:state_focused="true"/>
    <!-- 点击时的颜色 -->
    <item android:drawable="@drawable/shape_img_share_indicator_bg_selected" android:state_pressed="true"/>
    <!-- 默认颜色 -->
    <item android:drawable="@color/touming"/>
</selector>